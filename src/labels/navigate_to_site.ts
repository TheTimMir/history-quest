import { newLabel } from "@drincs/pixi-vn";

export const navigate_to_site = newLabel("navigate_to_site", [
    async () => {
        console.log("Navigating to another site...");

        // if (success) {
        //     console.log("✅ Результати успішно збережено в Firebase!");
        // } else {
        //     console.log("❌ Не вдалося зберегти результати в Firebase");
        // }
    },
]);
